import Discord from 'discord.js'

import parseAndFindChannel from '../utils/parse/parseAndFindChannel'

export default {
  startsWith: '!test',
  run: () => function (message: Discord.Message) {
  }
}